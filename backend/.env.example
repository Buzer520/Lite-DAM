# 数据库配置
DB_TYPE=sqlite
DB_PATH=./lite-dam.db

# JWT 配置（生产环境请修改为强密码）
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 服务器端口
PORT=3000

# 上传文件目录
UPLOAD_DIR=./uploads

# 存储配额（字节），默认 1GB
DEFAULT_STORAGE_QUOTA=1073741824

# Node 环境
NODE_ENV=production